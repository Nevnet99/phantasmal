{
	"name": "@phantasmal/marketing",
	"version": "0.0.1",
	"private": true,
	"license": "MIT",
	"type": "module",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "astro dev --port 4321",
		"build": "astro build",
		"preview": "astro preview",
		"typecheck": "astro check",
		"lint": "oxlint --ignore-pattern '**/*.astro' --ignore-pattern '.astro/**' .",
		"test": "echo 'No marketing unit tests yet'",
		"check": "bun run lint && bun run typecheck && bun run build"
	},
	"dependencies": {
		"@fontsource-variable/geist": "^5.3.0",
		"@fontsource-variable/geist-mono": "^5.3.0",
		"astro": "^7.3.2"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.4",
		"typescript": "^5.8.3"
	},
	"nx": {
		"tags": [
			"type:app",
			"scope:marketing"
		]
	}
}
